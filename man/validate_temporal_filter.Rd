% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postproc_checks.R
\name{validate_temporal_filter}
\alias{validate_temporal_filter}
\title{Validate temporal filtering (multitaper pre vs post)}
\usage{
validate_temporal_filter(
  pre_file,
  post_file,
  tr,
  band_low_hz = NA_real_,
  band_high_hz = NA_real_,
  mask_file = NULL,
  n_voxels = 30L,
  passband_loss_fail_db = 3
)
}
\arguments{
\item{pre_file}{Path to 4D BOLD before \code{temporal_filter}.}

\item{post_file}{Path to 4D BOLD after \code{temporal_filter}.}

\item{tr}{TR in seconds (\code{cfg$tr}).}

\item{band_low_hz}{Lower passband edge (Hz); \code{NA} if open.}

\item{band_high_hz}{Upper passband edge (Hz); \code{NA} if open.}

\item{mask_file}{Optional 3D mask; if unset, sample the whole volume.}

\item{n_voxels}{How many voxels to use.}

\item{passband_loss_fail_db}{Max allowed passband loss (dB) before fail (default 3).}
}
\value{
A logical scalar (\code{TRUE} if validation passed, \code{FALSE} if failed).
Attributes: \code{message}, \code{details} (numeric summaries and flags).
}
\description{
Band power outside / inside the passband; needs \code{multitaper} and \code{signal}.
}
\keyword{internal}
